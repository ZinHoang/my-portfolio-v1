import React from 'react'
import './FloatingNav.styles';

/**
 * FloatigNav is for tablet and mobile only. 
 * Might need to rename this to something like FloatingNavBar or FloatingNavMenu or FloatingMenu
 */
export const FloatingNav = () => {
  return <section id="floating-nav"></section>
}
